# Simple Tetris

A simple tetris game made entirely in c using the Raylib library.

### Compiling

Clone the repo and then run the following commands.

```bash
cd simple-tetris && make
```

Finally run the game.

```bash
./tetris
```

### Dependencies

The only dependency is Raylib.
